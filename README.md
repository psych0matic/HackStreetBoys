# HackStreetBoys
Hackathon Repo

To run this application:
1) Download/Clone the repository
2) Open a terminal in the root directory
3) type "Flutter run" to run a Windows App
   type "Flutter run -d web-server" to start a web server
